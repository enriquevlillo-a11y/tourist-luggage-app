spring.application.name=luggage-backend
server.port=5000

# Database Configuration - Uses environment variables from AWS
spring.datasource.url=${DATABASE_URL}
spring.datasource.driver-class-name=org.postgresql.Driver
spring.datasource.username=${DB_USERNAME}
spring.datasource.password=${DB_PASSWORD}
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false

# Disable SQL initialization in production
spring.sql.init.mode=never
spring.jpa.defer-datasource-initialization=true

# JWT Configuration from environment
jwt.secret=${JWT_SECRET}
jwt.expiration=${JWT_EXPIRATION:86400000}

# Production logging
logging.level.root=INFO
logging.level.com.dani.luggagebackend=INFO

# SSL Configuration (Production)
# server.ssl.enabled=true
# server.ssl.key-store=${SSL_KEY_STORE_PATH}
# server.ssl.key-store-password=${SSL_KEY_STORE_PASSWORD}
# server.ssl.key-store-type=PKCS12
# server.ssl.key-alias=luggage-app